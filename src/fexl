#!/bin/sh
./build || exit

# Disable core dump.
ulimit -Sc 0

# Limit stack and virtual memory in 1K blocks.
ulimit -Ss 20000
ulimit -Sv 100000

# Limit CPU time in seconds.
ulimit -St 5

../bin/fexl "$@"
