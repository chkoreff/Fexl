#
\if=(\x\y x y I)

# (check x y) = x if x is good, otherwise y.
\check=(\x \x=x is_good x x)

\error=
	(\msg
	fsay stderr msg
	die
	)

\form
define "check" check;
define "error" error;
define "if" if;
form
