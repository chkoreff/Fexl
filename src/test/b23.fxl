# Ensure automatic loading of libraries.  Each of these lines loads the next
# library file in the standard search order.
if F
(
say std use def if lib
and or not
neg max
map sort_all
as_str trim_spaces
say_data
get_key put_key
oct hex
tag tr
trace_elapsed
split_date day_of_week
grab_output
read_csv_file
read_ssv_file
)
