#
\neg=(- 0)
\max=(\x\y gt x y x y)
\min=(\x\y lt x y x y)

# Compute x modulo n.
\mod=(\x\n - x; * n; trunc; / x n)

\tau=(* 2 pi)

\form
define "max" max;
define "min" min;
define "mod" mod;
define "neg" neg;
define "tau" tau;
form
