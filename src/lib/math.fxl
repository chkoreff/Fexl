#
\neg=(- 0)
\max=(\x\y gt x y x y)
\min=(\x\y lt x y x y)

# Compute x modulo n.
\mod=(\x\n - x; * n; trunc; / x n)

\tau=(* 2 pi)

\round2=(\x / (round (* 100 x)) 100)

define "neg" neg;
define "max" max;
define "min" min;
define "mod" mod;
define "tau" tau;
define "round2" round2;
standard
